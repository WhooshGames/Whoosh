apiVersion: v1
kind: Service
metadata:
  name: django-api
  namespace: default
  labels:
    app: django-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: django-api

